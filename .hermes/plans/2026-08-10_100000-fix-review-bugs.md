# Fix Batch — Dogfood/Adversarial Review Bugs (2026-08-10)

> **Source:** dogfood-output/report.md + adversarial-ux-report.md (6 bugs + 1 GREEN ticket).
> **For Hermes:** implement all fixes, keep backend smoke ALL PASS + frontend lint/build green, verify live in both themes, commit with conventional commits + Co-Authored-By.

## Decisions (sequential-thinking pass, locked)

| # | Fix | Decision | Why |
|---|-----|----------|-----|
| 1 | Search relevance | Fuse `threshold 0.4→0.3`, keep `ignoreLocation:true`, `includeScore:true`; rank matches by score (dead-first → score asc → stars tiebreak) inside `filterStartups`; `page.tsx` skips the `sortStartups(...,"top")` re-sort when a query is present AND sort is still default `top`; explicit user sort still honored | threshold kills pure-fuzz false positives (obsidian→Avoca), keep location-agnostic for deep-description recall; relevance must win for the core "does X exist?" question |
| 2 | Duplicate entries | `db.py`: add `normalize_url()` (lowercase scheme+host, strip `www.`, default ports, trailing `/`, query, fragment); `find_by_url` keeps exact-match fast path, adds normalized fallback scan. `seeder._topstartups`: skip own-domain candidates (topstartups.io) + within-run seen-set. One-off merge with DB backup: Zoom keep id 7 / delete 22; Pogo keep id 37 / delete 31 (live site title confirms id 37's profile); transfer `verify_log` rows | zoom.com vs zoom.com/ root cause = no URL normalization; Pogo id 31 = scraper captured its own domain + hallucinated LLM profile |
| 3 | Modal focus restore | `startup-detail.tsx`: restore `lastFocused` on close via a `wasOpen` ref (restore only when closing, not on similar-item swaps — no focus flicker mid-navigation) | cleanup currently only resets overflow; missing `lastFocused.current?.focus?.()` |
| 4 | Add-form feedback | Button disabled only while busy; handlers validate: empty → inline "Enter a … URL first."; bad format (`new URL`, http/https only) → "That doesn't look like a valid URL…"; live error, `aria-invalid` + `aria-describedby` + `role=alert` | Deviates from review's "disable on invalid" — a clickable button that answers beats a dead one (persona clicked the dead button 3×) |
| 5 | 404 page | New `frontend/app/not-found.tsx`, themed, curator voice, link home | stock Next 404 = dead end |
| 6 | Grid row variance | Card tagline gains `line-clamp-2 min-h-9` (2-line box) → uniform 311px rows | 293px rows caused by 1-line taglines |
| B | Trust legend (GREEN) | One line under the hero stat: verified/unverified/dead dots + words | addresses "why is half the grid grey" cold-start confusion; additive |

## Open questions (defaults chosen — all low-risk/reversible)

1. Pogo merge direction → keep joinpogo.com row (verified against live site). *(default)*
2. Add-form: clickable-with-error vs disabled-with-hint → clickable-with-error. *(default)*
3. Search + explicit sort: relevance wins only while sort is default "top". *(default)*
4. Legend included? → yes, one line. *(default)*
5. Commit? → yes, per repo pattern (conventional + Co-Authored-By: Hermes Agent). *(default)*

## Files touched

- `backend/app/db.py` — `normalize_url()` + `find_by_url` normalized fallback
- `backend/app/seeder.py` — topstartups own-domain + within-run dedupe guards
- `frontend/lib/search.ts` — threshold, includeScore, relevance ranking
- `frontend/app/page.tsx` — results memo conditional; trust legend
- `frontend/components/startup-detail.tsx` — focus restore
- `frontend/components/add-startup-dialog.tsx` — validation + inline errors
- `frontend/components/startup-card.tsx` — tagline clamp
- `frontend/app/not-found.tsx` — NEW themed 404
- Data: `backend/data/ideasexist.db` — merge 2 dupes (backup first: `ideasexist.db.bak-20260810`)

## Verification (canonical + live)

1. `npm test` at root — backend smoke ALL PASS (pinned: `_topstartups` output equality + `find_by_url` dedup) + frontend lint/build.
2. Live browser, both themes: search `obsidian` → Obsidian first, no false positives; `zoom` → 1 card; add-dialog errors on empty/garbage; Escape restores focus; 404 themed; all rows 311px; legend renders.
