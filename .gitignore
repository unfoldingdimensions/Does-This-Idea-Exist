# deps
node_modules/
.next/
out/

# python
backend/.venv/
__pycache__/
*.pyc

# secrets & local data
.env
backend/.env
frontend/.env.local
backend/data/*
!backend/data/seed_famous.json
!backend/data/seed_design_library.json

# misc
.DS_Store

# runtime logs
*.log

# agent/session working state — machine-local, never ship
.agents/
.mimosa/
.zcode/
.hermes/
.impeccable/
.openclaw/
.openclaw-attachments/
AGENTS.md
SOUL.md
IDENTITY.md
USER.md
TOOLS.md
HEARTBEAT.md

# screen recordings / video captures — large binaries, stay local
video tests/

# QA output — ad-hoc runs stay local. The reviewed reports and the screenshots
# they cite are project history (referenced from PRODUCT.md) and stay tracked;
# the blanket `dogfood-output/` rule used to contradict those 6 committed files.
# Allow-listed explicitly, same pattern as backend/data above, so a stray new
# screenshot doesn't get swept in by `git add -A`.
.dogfood-output/
dogfood-output/*
!dogfood-output/*.md
!dogfood-output/screenshots/
dogfood-output/screenshots/*
!dogfood-output/screenshots/dup-zoom-search.png
!dogfood-output/screenshots/home-dark.png
!dogfood-output/screenshots/home-initial.png
!dogfood-output/screenshots/search-obsidian-relevance.png

# lighthouse QA artifacts
scripts/lh-*.json

# planning, handover & working documents — local-only by decision (2026-09-15)
# The build specs and the phase ledger are deliberately NOT here: the phase
# prompts read them and Phase 1 needs docs/phase-ledger.md on main.
implementation-plan.md
docs/handoff.md
docs/prompts/
revamp-plan.md
reworked-revamp-plan.md
revamp-plan-delta.md
Revamp-thinking.txt
docs/discussion-record.md
docs/revamp-documents-review.md
deliverables-index.md

# memory items
MEMORY.md
memory/

# Command Code tool state (machine-local)
.commandcode/
